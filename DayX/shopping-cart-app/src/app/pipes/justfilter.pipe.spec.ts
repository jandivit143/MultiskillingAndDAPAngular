import { JustfilterPipe } from './justfilter.pipe';

describe('JustfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new JustfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
